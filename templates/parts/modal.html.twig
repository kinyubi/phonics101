{% extends '../base.html.twig' %}
{# inputs:
    modalId:  the id of the modal element
    buttonText: the text on the modal button
#}

{% block modalButton %}
    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#{{ modalId }}">{{ buttonText }}</button>
{% endblock %}

{# inputs:
    modalId:  the id of the modal element
    title:    the text in the title section of the modal
    bgTitle:  Title background (default is secondary)
    modalBody: raw HTML
    modalFooter: raw HTML
#}
{#{% block modalVerticalCentered %}#}
{#    <div class="modal" id="{{ modalId }}" tabindex="-1" role="dialog"#}
{#         aria-labelledby="{{ modalId }}Label" aria-hidden="true">#}
{#        <div class="modal-dialog modal-dialog-centered" role="document">#}
{#            <div class="modal-content">#}
{#                <div class="modal-header {{ bgTitle|default('bg-secondary')}} text-light">#}
{#                    <h5 class="modal-title" id="{{ modalId }}Label">{{ title }}</h5>#}
{#                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">#}
{#                        <span aria-hidden="true">&times;</span>#}
{#                    </button>#}
{#                </div>#}
{#                <div class="modal-body">#}
{#                    {{ modalBody|raw }}#}
{#                </div>#}
{#                <div class="modal-footer">#}
{#                    {{ modalFooter|raw }}#}
{#                </div>#}
{#            </div>#}
{#        </div>#}
{#    </div>#}
{#{% endblock %}#}

{# relevant id's:
    modal Id:        modalError
    modal title Id:  modalErrorLabel
    modal text Id:   modalErrorText
#}
{% block modalError %}
    <div class="modal" id="modalError" tabindex="-1" role="dialog"
         aria-labelledby="modalErrorLabel" aria-hidden="true" style="width: 40%">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header bg-danger text-light">
                    <h5 class="modal-title" id="modalErrorLabel">{{ title }}</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div id="modalErrorText" class="modal-body">
                    {{ modalErrorHtml|raw }}
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-primary" data-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{# parameters:
    target : the modalId in a modalIframe block
    title
#}
{% block modalIframeButton %}
    <button type="button" class="btn btn-readxyz" data-toggle="modal" data-target="#{{ target }}">{{ title }}</button>
{% endblock %}

{# relevant id's:
    modal Id:        modalSuccess
    modal title Id:  modalSuccessLabel
    modal text Id:   modalSuccessText
#}
{#
{% block modalSuccess %}
    <div class="modal" id="modalSuccess" tabindex="-1" role="dialog"
         aria-labelledby="modalSuccessLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header bg-danger text-light">
                    <h5 class="modal-title" id="modalSuccessLabel">{{ title }}</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div id="modalSuccessText" class="modal-body">
                    {{ modalSuccessHtml|raw }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
#}
{# relevant id's:
    modal Id:        modalWarning
    modal title Id:  modalWarningLabel
    modal text Id:   modalWarningText
#}
{#
{% block modalWarning %}
    <div class="modal" id="modalWarning" tabindex="-1" role="dialog"
         aria-labelledby="modalWarningLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header bg-danger text-light">
                    <h5 class="modal-title" id="modalWarningLabel">{{ title }}</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div id="modalWarningText" class="modal-body">
                    {{ modalWarningHtml|raw }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
#}
{% block statusModals %}
    {{ block('modalError')|raw }}
{#    {{ block('modalSuccess') }}#}
{#    {{ block('modalWarning') }}#}
{% endblock statusModals %}

{% block body %}
    {{ block('modalButton') }}
    {{ block('modalVerticalCentered') }}
{% endblock %}
